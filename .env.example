# MCP Feedback Collector 環境配置
# 複製此文件為 .env 並根據需要修改

# ===== Web 服務器配置 =====
# Web 服務器端口（預設：3239）
MCP_WEB_PORT=3239

# CORS 來源設定（預設：*）
MCP_CORS_ORIGIN=*

# 伺服器主機（可選）
# MCP_SERVER_HOST=localhost

# 伺服器基礎 URL（可選）
# MCP_SERVER_BASE_URL=http://localhost:3239

# 強制使用指定端口（預設：false）
MCP_FORCE_PORT=false

# 使用固定 URL（預設：true）
MCP_USE_FIXED_URL=true

# ===== 超時配置 =====
# 反饋對話超時時間（秒，預設：60000）
MCP_DIALOG_TIMEOUT=60000

# ===== 日誌配置 =====
# 日誌級別：silent, error, warn, info, debug（預設：info）
LOG_LEVEL=info

# ===== 圖片處理配置 =====
# 最大檔案大小（位元組，預設：10485760 = 10MB）
MCP_MAX_FILE_SIZE=10485760

# 圖片壓縮品質（1-100，預設：80）
IMAGE_QUALITY=80

# 圖片轉文字功能開關（預設：true）
MCP_ENABLE_IMAGE_TO_TEXT=true

# 圖片轉文字提示詞（預設：詳細描述提示）
MCP_IMAGE_TO_TEXT_PROMPT=請詳細描述這張圖片的內容，包括主要元素、顏色、佈局、文字等資訊。

# ===== AI API 配置（可選，用於圖片描述功能）=====
# API 基礎 URL
MCP_API_BASE_URL=https://api.openai.com/v1

# API 密鑰
MCP_API_KEY=your-api-key-here

# AI 模型名稱
MCP_DEFAULT_MODEL=gpt-4o-mini
